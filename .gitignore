# Node
node_modules/
dist/
*.tsbuildinfo
.pnpm-debug.log*

# Rust / Anchor
target/
Cargo.lock
.anchor/
test-ledger/

# Spike crates pin their own Cargo.lock for reproducibility
!_spikes/**/Cargo.lock

# IDLs are generated, not committed
packages/programs-core/target/idl/

# Build artifacts
*.so
*.wasm

# Pre-rotation deploy keypair backups (snapshot dirs created when
# target/deploy/*-keypair.json get rotated; the rotation pubkeys are
# documented in commit messages, the backup files contain private
# keys and must never be committed).
.deploy-keypair-backup-*/

# Editor
.vscode/
.idea/
*.swp
.DS_Store

# Per-user agent state (Claude Code skills, hooks, scratch). Not
# shared — different developers run different tooling, and
# project-scoped agent state causes merge churn. Pattern follows
# `.cursor/`, `.aider/`, etc.
.claude/

# Env
.env
.env.local
.env.*.local

# Logs
*.log
